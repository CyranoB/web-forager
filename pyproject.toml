[build-system]
requires = ["setuptools==75.8.0", "wheel==0.45.1", "setuptools_scm==8.2.0"]
build-backend = "setuptools.build_meta"

[project]
name = "web-forager"
dynamic = ["version"]
description = "A search-and-fetch toolkit for AI agents — MCP server and standalone Agent Skills powered by DuckDuckGo, trafilatura, and Jina Reader"
readme = "README.md"
authors = [
    {name = "Eddie Pick", email = "eddie@pick.fr"}
]
license = {text = "MIT"}
requires-python = ">=3.10,<3.14"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Internet :: WWW/HTTP :: Indexing/Search",
]

dependencies = [
    "mcp>=1.9.0",
    "fastmcp>=2.3.4",
    "requests>=2.28.0,<2.32.0",
    "urllib3>=1.26.0,<2.0.0",
    "beautifulsoup4>=4.11.0",
    "trafilatura>=2.0.0",
    "ddgs>=9.5.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "isort>=5.0.0",
    "mypy>=1.0.0",
    "pytest-cov>=4.0.0"
]

[project.scripts]
web-forager = "web_forager.cli:main"

[project.urls]
Homepage = "https://github.com/CyranoB/web-forager"
Repository = "https://github.com/CyranoB/web-forager"
Issues = "https://github.com/CyranoB/web-forager/issues"


[tool.setuptools.packages.find]
where = ["src"]
include = ["web_forager", "web_forager.*"]

[tool.setuptools_scm]
write_to = "src/web_forager/_version.py"
version_scheme = "post-release"
local_scheme = "no-local-version"  # This removes the local version component

[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = ["src/web_forager/_version.py"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "SIM", "UP", "N", "S", "A", "C90", "RUF"]
