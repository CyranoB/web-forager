# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

name: web-forager
displayName: Web Forager
description: Search the web using DuckDuckGo and fetch/convert web content using Jina Reader. Privacy-focused search with no API key required.
visibility: public
type: mcp
author:
  name: Eddie Pick
  url: https://github.com/CyranoB
repository: https://github.com/CyranoB/web-forager
keywords:
  - search
  - duckduckgo
  - web
  - fetch
  - jina
  - privacy
files:
  - README.md
  - pyproject.toml
  - src/web_forager/server.py
  - src/web_forager/duckduckgo_search.py
  - src/web_forager/jina_fetch.py

startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      debug:
        type: boolean
        description: Enable debug logging
        default: false
  commandFunction: |-
    (config) => ({
      command: 'uvx',
      args: config.debug
        ? ['--python', '>=3.10,<3.14', 'web-forager', 'serve', '--debug']
        : ['--python', '>=3.10,<3.14', 'web-forager', 'serve']
    })
  exampleConfig:
    debug: false
